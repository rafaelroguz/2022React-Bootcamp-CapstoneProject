export const selectCartProducts = (state) => state.cart.products;
export const selectCartProductsIds = (state) => state.cart.productsIds;
export const selectIsLoadingCart = (state) => state.cart.isLoading;
